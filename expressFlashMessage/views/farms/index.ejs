<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Farms Index - All Farms</title>
  </head>
  <body>
    <!--messages is arrayObject of messages stored in flash area of session-->
    <!-- if no message were stored in flash area of session - messages is an empty arrayObject -->
    <%= messages %>
    <!--clears messages stored in flash area of session (ie arrayObject) after viewing-->

    <h1>All Farms</h1>
    <ul>
      <% for(let f of farms){%>
      <li><a href="/farms/<%=f._id%>"><%=f.name%></a></li>
      <%}%>
    </ul>
    <a href="/farms/new">Add Farm</a>
    <!--(http structured) GET request-->
  </body>
</html>
